<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rootrip.rootripteam.recommendLoc.RecommendLocMapper">
	<select id="getLocByCate" parameterType="BigDecimal" resultType="BigDecimal">
		select L_NO from SPOT where C_NO = #{a} group by L_NO
	</select>
</mapper>